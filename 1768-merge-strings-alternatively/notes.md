Initial/Optimal:
- Pretty straightforward question so my initial was the most optimal already.
- Runtime is O(n) because you're looping through the string
- Memory is O(1) because only remembering "constant" values
- Loop through all the characters of the largest string. Once you reach the breaking point, only append one character using conditionals.
